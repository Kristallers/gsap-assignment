import React from "react";
import styles from "./styles/Hero.module.css";

const Hero = () => {
	return (
		<>
			<div className={styles.heroHeaderContainer}>
				<h1 className={styles.header}>BINKY.</h1>
			</div>
		</>
	);
};

export default Hero;
